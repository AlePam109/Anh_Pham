# Immersive 3D Portfolio

This project is scaffolded with Vite + React and configured for React Three Fiber and Tailwind CSS.

## Development

```bash
npm install
npm run dev
```

Use OrbitControls to navigate the scene. Future rooms can be added under `src/components`.
